#
# Configuration file for DICOM viewer window level definitions.
#
# Do not change files in the config/config directory. These are default template files.
# If you want to edit a configuration file, copy the default template file from the config/config
# directory to the config/local_config directory and modify the file copy.
#
# Please note that it is required to test any configuration changes. This is done by:
#
# 1. Export an examination and the DICOM viewer to a disk location.  
# 2. Run the viewer from the disk location and verify that the changes have taken effect.
#
# If the viewer doesn't start properly it is likely that the changes to the configuration 
# file were made incorrectly.
# To remedy such problems, remove the modified file from the config/local_config directory.
#
# If the default template files are corrupted, remove the entire config directory 
# including sub folders "config" and "local_config". 
# The folder structure will be re-created during the next export from IDS7.
# 


Window_level {
    # The sensitivity when changing window level can be set differently for 
    # different modalities. But this can also be modified using any suitable 
    # dicom element for the site. E.g. instead of using [8,60,CS] the 
    # station name [8,1010,SH] can be used.
    # 
    # A higher sensitivity value will make the window level change 
    # more rapidly. Shift_sensitivity_multiplier determines how the W/L change
    # rate will be affected by pressing Ctrl. A value of 0.2 will for instance
    # mean that the change rate will be five times slower when pressing Ctrl.

    Modality_switch <[8,60,CS]>
    * {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    CR {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    CT {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    DR {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    MG {
       Sensitivity <1.0>
       Shift_sensitivity_multiplier <0.2>
    }
    MR {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    US {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    NM {
       Sensitivity <100.0>
       Shift_sensitivity_multiplier <0.2>
    }
    Cheat_limit <1500000>
    Min_color_usage <0.3>
}
Local_windows {
    CT {
	1 {
	    Label <Bone>
	    Width <800>
	    Center <600>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
	}
	2 {
	    Label <Tumour tissue>
	    Width <40>
	    Center <70>
            AcceleratorKey <SI_F2>
            AcceleratorLabel <F2>
	}
	3 {
	    Label <Coagulated blood>
	    Width <20>
	    Center <65>
            AcceleratorKey <SI_F3>
            AcceleratorLabel <F3>
	}
	4 {
	    Label <Grey brain substance>
	    Width <10>
	    Center <41>
            AcceleratorKey <SI_F4>
            AcceleratorLabel <F4>
	}
	5 {
	    Label <White brain substance>
	    Width <10>
	    Center <27>
            AcceleratorKey <SI_F5>
            AcceleratorLabel <F5>
        }
	6 {
	    Label <Scanogram>
	    Compute <min-max>
            AcceleratorKey <SI_F6>
            AcceleratorLabel <F6>
	    DefaultForImageType <LOCALIZER>
        }
    }
    OT {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
	2 {
	    Label <VIDAR Scanner>
	    Width <2500>
	    Center <2400>
            AcceleratorKey <SI_F2>
            AcceleratorLabel <F2>
        }
    }
    MR {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
    CR {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
    DR {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
    US {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
    MG {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
    NM {
	1 {
	    Label <Min-Max>
	    Compute <min-max>
            AcceleratorKey <SI_F1>
            AcceleratorLabel <F1>
        }
    }
}


#
# Default window.
#
# It is possible to set the default window level settings on modality level.
# The value of "window" is either one of the defined "Local windows" specified
# by the local window number or the text "IMAGE" that indicates that the 
# window width and window center values will be fetched from the image.
#
# -IMAGE
#  If "IMAGE" is used as "window" parameter, the "value" parameter specifies 
#  which of the image's window width and window center
#  (there can be several) that should be used (If there are four values, 
#  possible settings are 1, 2, 3 or 4). 
# -Local window
#  If a local window is used, the "value" parameter will have no effect.
#
# Note: If a VOI LUT sequence is used (enabled) this configuration will
# have no effect at all. To enable/disable the use of the VOI LUT sequence,
# modify manufacturer_specific.def in this directory.
#

Default_window {
  MG {
    window<IMAGE>
    value<1>
  }
  OT {
    window<1>
  }

}
